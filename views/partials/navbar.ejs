<!-- Navigation Bar -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
    <div class="container">
        <span class="navbar-brand fw-bold" id="navbarTitle">Reaproveitamento</span>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link text-white" href="/dashboard"><i class="bi bi-person-circle me-1"></i>Perfil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white" href="/doacoes"><i class="bi bi-box2-heart me-1"></i>Doações Disponíveis</a>
                </li>
                <% if (!user.admin) { %>
                <li class="nav-item">
                    <a class="nav-link text-white" href="/doacoes/nova"><i class="bi bi-plus-circle me-1"></i>Doar Item</a>
                </li>
                <% } %>
                <li class="nav-item">
                    <a class="nav-link text-white" href="/historico"><i class="bi bi-clock-history me-1"></i>Histórico</a>
                </li>
                <% if (user.admin) { %>
                    <li class="nav-item">
                        <a class="nav-link text-warning fw-bold" href="/admin/armazenagem"><i class="bi bi-shield-lock me-1"></i>Controle de Armazém</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link text-warning fw-bold" href="/admin/sorteios"><i class="bi bi-gift-fill me-1"></i>Sorteios</a>
                    </li>
                <% } %>
            </ul>
            <div class="d-flex align-items-center justify-content-end">
                <% if (!user.admin) { %>
                <span class="badge bg-success p-2 px-3 me-3 fs-6 border border-light border-opacity-10 d-flex align-items-center shadow-sm">
                    <i class="bi bi-wallet2 me-2"></i>
                    Saldo: R$ <%= parseFloat(user.credits).toFixed(2).replace('.', ',') %>
                </span>
                <% } %>
                <span class="text-white-50 me-3 d-none d-sm-inline">
                    Olá, <strong><%= user.name || 'Usuário' %></strong>
                </span>
                <a href="/logout" id="logoutBtn" class="btn btn-outline-light btn-sm"><i class="bi bi-box-arrow-right me-1"></i>Sair</a>
            </div>
        </div>
    </div>
</nav>
